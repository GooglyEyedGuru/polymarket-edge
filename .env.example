# Polymarket
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=
POLYMARKET_WALLET_PRIVATE_KEY=    # Polygon wallet private key

# Goldsky (subgraph queries)
GOLDSKY_API_KEY=your_key_here

# Telegram alerts
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Risk config
BANKROLL_USDC=1000               # Total capital allocated
MAX_POSITION_PCT=0.02            # 2% Kelly cap per market
MAX_TOTAL_EXPOSURE_PCT=0.15      # 15% max total open
MAX_SINGLE_BUCKET_PCT=0.05       # 5% per resolution bucket
DAILY_LOSS_LIMIT_PCT=0.04        # 4% → pause 24h
MAX_CONCURRENT_POSITIONS=8

# Edge thresholds
MIN_EDGE_PCT=7                   # Minimum edge to consider
MIN_EDGE_SPONSORED_PCT=4         # Lower bar for reward markets
MIN_CONFIDENCE=70                # Minimum confidence (0-100)
AUTO_EXECUTE_EDGE_PCT=12         # Auto-execute above this
AUTO_EXECUTE_CONFIDENCE=85       # Auto-execute above this
AUTO_EXECUTE_MAX_SIZE_PCT=1      # Auto-execute only if <1% bankroll

# Scan config
SCAN_INTERVAL_MINUTES=10
MIN_MARKET_VOLUME=10000          # $10k lifetime volume
MIN_MARKET_LIQUIDITY=1000        # $1k depth on our side
MIN_EXPIRY_MINUTES=60            # Ignore markets expiring in <60min
